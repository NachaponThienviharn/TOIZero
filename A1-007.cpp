#include <bits/stdc++.h>

using namespace std;

int main() {
  char al;
  cin >> al;
  if (al == 'a' || al == 'e' || al == 'i' || al == 'o' || al == 'u') {
    cout << "yes";
  } else {
    cout << "no";
  }
}